Observer.log contains the logs coming from the daemon
that reads the log updates.


Athanor.log contains the logs coming from the daemon
that handles the tree, so it contains the Alerts launched and the Tree states when updated.